<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syy.springboot.mapper.AuthorityUserRoleMapper">
    <resultMap id="BaseResultMap" type="com.syy.springboot.model.AuthorityUserRole">
        <id column="user_id" jdbcType="INTEGER" property="userId"/>
        <id column="role_id" jdbcType="INTEGER" property="roleId"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="com.syy.springboot.model.AuthorityUserRole">
    delete from authority_user_role
    where user_id = #{userId,jdbcType=INTEGER}
      and role_id = #{roleId,jdbcType=INTEGER}
  </delete>
    <delete id="deleteByUserId">
         delete from authority_user_role
        where user_id = #{userId}
    </delete>
    <select id="queryByUserId" resultType="com.syy.springboot.model.AuthorityUserRole">
        SELECT user_id userId,role_id roleId FROM authority_user_role WHERE role_id=#{roleId}
    </select>
    <insert id="insertSelective">
        insert into authority_user_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                user_id,
            </if>
            <if test="roleId != null">
                role_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                #{userId,jdbcType=INTEGER},
            </if>
            <if test="roleId != null">
                #{roleId,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <insert id="insertAuthorityUserRole">
        insert into authority_user_role (user_id,role_id) values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.userId},
            #{item.roleId})
        </foreach>
    </insert>
</mapper>